jenkins:
  securityRealm:
    local:
    allowsSignup: false
    users:
    - id: ${JENKINS_ADMIN_ID}
      password: ${JENKINS_ADMIN_PASSWORD}
unclassified:
  location:
      # (server_ip is the private ip)
      url: http://172.31.7.79:8080/ 